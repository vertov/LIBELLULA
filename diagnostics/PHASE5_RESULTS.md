# Phase 5 — Re-run Comparative Check

Not executed. Phases 1–4 never observed `pred_valid` under any stimulus (even with hash-aligned events and must-fire assertion), so there is no emitting configuration to carry forward into comparative testing. `diagnostics/out/phase5_subset_results.csv` records the phase as **SKIPPED** with reason “No pred_valid observed in phases 1–4.”
